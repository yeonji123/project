import React from 'react';
import {View, Text, StyleSheet, TouchableOpacity} from 'react-native';

const StationWorking = ({navigation}) => {
    return (
        <View>
            
        </View>
    );
};

export default StationWorking;